/**************** Methods of RegExp & Methods of String ***************************

methods of RegExp
    exec()
    test() 
methods of String
    match()
    matchAll()
    replace()
    replaceAll()
    search()
    split() 

********************************************************************/

/********************** EXPLANATION *********************************
    Regex.test()
    - returns true/false only
    - true = when there is a match between regular expression & the string
    - false = all other cases

    Regex.exec()
    - returns null/array
    - null  = when no match is found
    - array = the returned array has matched text as the first item

    String.match()
    - returns null (or) array
    - null = when no match is found
    - array = the returned array has matched text as the first item

    String.search()
    - returns index of the match (or) -1 if no match is found
        
    Others
    - blah blah

********************************************************************************/