/* 

check if JSON
    try { json.parse() } catch(e) { return false }

JSON string
JSON Object literal
JSON Object

check circular json

for... of 
    can not iterate over an Object
    introduced in ES6
    for (let [key,value] of Object.entries() ) { }

for... in
    iterate through the keys of an object... 
    for (let key in someObject23) { }


*/