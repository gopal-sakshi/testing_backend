const fullName = '  Kate Smith  ';
fullName.trim(); // => 'Kate Smith'

// removes the spaces from both the start and end of the string, 
// if a whitespace is found in between two letters, then, of course, this whitespace is preserved:


