// objects are passed as reference
// if a function modifies the argument/parameter it received... 
    // that change is visible outside


// new Date() =======> date in ISO format; methods ===> getTime() returns date in milliseconds, setTime()
// Date.now() =======> date in milliseconds

/*

Math 
- cant use constructor... or new keyword
- all methods are static
- thats why we use all methods directly
    Math.floor(); Math.ceil()

null_vs_undefined
    var x;          typeof x ======> undefined  (undefined is a datatype on its own)
    var y=null;     typeof y ====> object
    undefined ===> we created a variable; but didnt assign a value to it... 'JAVASCRIPT' itself assigns undefined to that variable
    null ========> we must explicitly set a variable to null; it means the variable's value is empty/blank
*/