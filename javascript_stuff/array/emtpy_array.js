var emptyArray1 = [];

if (emptyArray1) {                      // even though array is empty... "if block" is executed
    console.log(emptyArray1);
} else {
    console.log('even though empty array, if doesnt mean array is NULL')
}

// sometimes emptyArray1 can be [] (or) ''          // empty_array (or) empty_object
